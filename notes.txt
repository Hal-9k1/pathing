vizgraph
  check if line segment intersects rectangle
  should tolerate lines that are sides of the rectangle
  line segment expressed as two points
  remap to rectangle space (pick two orthogonal sides as unit vectors
  rectangle is now unit square and segment has been linearly transformed
  problem is now checking if line segment intersects unit square
  intersection if any are true
    x = 0 and 0 < y < 1
    x = 1 and 0 < y < 1
    y = 0 and 0 < x < 1
